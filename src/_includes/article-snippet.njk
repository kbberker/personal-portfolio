<li>
  <article>
    <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
    <p>by <span>{{ post.data.author }}</span> on <span>{{ post.data.date | formatDate }}</span></p>
    <p>{{ post.data.description }}</p>
    <a href="{{ post.url }}">Continue reading</a>
  </article>
</li>